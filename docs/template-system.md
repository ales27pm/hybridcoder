# Template System

The old template layer is too close to a demo sandbox. The refactor should move HybridCoder to multi-file Expo templates with explicit manifests and options.

## Template goals

Each template should define:

- app category
- file scaffold
- dependency expectations
- navigation pattern
- theme baseline
- starter options
